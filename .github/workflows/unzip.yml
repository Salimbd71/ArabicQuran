name: Unzip ZIP to Repo

on:
  workflow_dispatch:  # ম্যানুয়ালি চালানোর জন্য

jobs:
  unzip-and-push:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout repo
      - name: Checkout repository
        uses: actions/checkout@v3

      # 2️⃣ যদি ZIP ফাইল রিপোতে থাকে
      - name: Unzip local ZIP
        run: |
          unzip assets/myfiles.zip -d .
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add .
          git commit -m "Unzip files to repo root"
          git push

      # 2️⃣ যদি ZIP ফাইল URL থেকে ডাউনলোড করতে হয়
      # - name: Download & Unzip
      #   run: |
      #     curl -L -o myfiles.zip https://example.com/myfiles.zip
      #     unzip myfiles.zip -d .
      #     git config user.name "github-actions"
      #     git config user.email "github-actions@github.com"
      #     git add .
      #     git commit -m "Unzip files to repo root"
      #     git push
